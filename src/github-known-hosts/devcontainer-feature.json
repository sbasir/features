{
  "id": "github-known-hosts",
  "version": "1.1.0",
  "name": "GitHub Known Hosts",
  "description": "Adds GitHub SSH host keys (RSA, ECDSA, ED25519) to root and/or remoteUser known_hosts",
  "documentationURL": "https://github.com/sbasir/features",
  "options": {
    "addForRoot": {
      "type": "boolean",
      "default": true,
      "description": "Add GitHub host keys to /root/.ssh/known_hosts"
    },
    "addForRemoteUser": {
      "type": "boolean",
      "default": true,
      "description": "Add GitHub host keys to remoteUser's ~/.ssh/known_hosts"
    }
  },
  "installsAfter": []
}
